- name: Ansible Copy WAR CI to Remote
  hosts: 127.0.0.1
  tasks:
    - name: copying war with playbook
      become: true 
      copy:
        src: target/maven-web-application.war
        dest: /opt/tomcat/webapps/
        owner: root
        group: root        
        mode: 0644

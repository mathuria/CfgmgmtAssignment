- name: Deploy and restart
  hosts: webcontainers
  
  tasks:
    - name: Deploy
      copy:
        src: target/maven-web-application.war
        dest: /opt/tomcat/webapps/
        owner: tomcat
        group: tomcat        
        mode: 0644
